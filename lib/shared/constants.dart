import 'package:flutter/material.dart';

class Constants {
  // DB
  static const user = 'user';
  static const userId = 'userId';
}

class AppColors {
  // Light theme
  static const Color primaryColorLight = Color.fromRGBO(75, 75, 75, 1);
  static const Color accentColorLight = Color.fromRGBO(40, 97, 235, 1);
  static const Color backgroundGrey = Color.fromRGBO(239, 239, 243, 1);
  static const Color black = Color.fromRGBO(23, 30, 33, 1);
  static const Color white = Colors.white;

  // static const Color mainIconColor = Color.fromRGBO(0, 204, 0, 1);
  static const Color yellow = Color.fromRGBO(230, 200, 0, 1);

  // Dark theme
  static Color primaryColorDark = const Color.fromRGBO(23, 30, 33, 1);
  static const Color accentColorDark = Colors.deepPurpleAccent;
}
